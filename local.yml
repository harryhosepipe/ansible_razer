---
- hosts: localhost
  become: true
    
  tasks:
    - name: Install Base
      pacman:
        name:
          - wget
          - git
          - curl
          - gnupg
        state: present

    # Your tasks here

            #- import_tasks: gitsetup.yml

